// Placeholder route for the centre tab slot. Pressing the tab is intercepted in
// the tab navigator and opens the camera modal instead, so this never renders.
export default function ScanPlaceholder() {
  return null;
}
